// This line enables strict mode, which helps catch common mistakes and unsafe actions in the code
// It makes sure the code runs in the latest version of the JavaScript engine
"use strict";

// The following line is commented out because 'alert' is not used in Node.js
// alert(8 + 9)

// Instead of 'alert', we use 'console.log' to show the result in Node.js
// 'alert' is used in browsers to show a popup with the result
console.log(8 + 9);

// Declare a variable 'name' with a value of "Suresh beekhani"
let name = "Suresh beekhani"

// Declare a variable 'age' with a value of 18
let age = 18

// Declare a variable 'isLoggedIn' with a value of false
let isLoggedIn = false

// Declare a variable 'state' without an initial value
let state;

// Different data types in JavaScript:
// number => represents numeric values
// bigint => represents large integers
// string => represents text values
// boolean => represents true or false
// null => represents an empty or non-existent value
// undefined => represents a variable that has been declared but not assigned a value
// symbol => represents a unique value

// object => represents a collection of properties

// Print the type of 'undefined' to the console, which is 'undefined'
console.log(typeof undefined); // undefined

// Print the type of 'null' to the console, which is 'object'
console.log(typeof null); // object
